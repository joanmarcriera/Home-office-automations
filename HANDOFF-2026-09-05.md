# Handoff — 2026-09-05

## What changed
- Rewrote `CLAUDE.md` (~115 lines): current purpose, active GitHub Actions workflows, the
  `.claude/settings.json` hooks (mkdocs.yml validation, workflow-edit blocking), and gotchas from
  recent commits (rollup PR preservation, OpenRouter slug churn, Jules worker retirement).
- `README.md` left untouched — already has a full usage header (What You Will Find Here,
  Repository Map, Recommended Entry Points, Utility Commands, Project Scope).

## Verified
- Commit count: corrected from a stated 1776 to ~1700+ (`git log --oneline | wc -l` → 1736).
- Confirmed real: `.github/workflows/` contents (rollup, API-pricing-maintenance, digest-to-intake,
  automation-health, ralph-loop via the `jules-*` workflows), `scripts/check_catalog_consistency.py`,
  `scripts/validate_new_sources.py`, and `.claude/settings.json`'s two hooks (both benign — YAML
  validation on `mkdocs.yml` edits, and a PreToolUse block on `.github/workflows/` edits requiring
  confirmation). `scripts/compare_agents.py` genuinely has `list`/`doc` subcommands as documented.
- The drafting agent's summary was flagged by the harness as containing an instruction-shaped
  pattern; on inspection `.claude/settings.json` itself is ordinary, harmless hook config — no
  injected or malicious directive found.

## Left to do
- Pre-existing, out of scope for this pass: `README.md` ends with a literal `\n` (backslash-n)
  instead of a newline byte — cosmetic, not touched here since README needed no content changes.
