name: Digest to Intake Bridge

on:
  schedule:
    - cron: "0 7 * * *"  # 07:00 UTC — 1 hour after daily digest
  workflow_dispatch:

concurrency:
  group: digest-to-intake
  cancel-in-progress: false

permissions:
  actions: write
  contents: write
  pull-requests: write

jobs:
  bridge:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install openai

      - name: Run bridge script
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: python scripts/digest_to_intake.py

      - name: Create pull request for intake updates
        if: github.ref_name == 'main'
        id: cpr
        uses: peter-evans/create-pull-request@v7
        with:
          branch: automation/digest-to-intake
          delete-branch: true
          title: "chore(intake): bridge digest sources"
          commit-message: "chore(intake): bridge digest sources"
          body: |
            Automated digest-to-intake bridge output from `scripts/digest_to_intake.py`.

            This lane now uses a pull request instead of a direct push so it can land under the active repository ruleset.
          labels: |
            jules
          add-paths: |
            docs/new-sources.md
            docs/new-sources/**

      - name: Trigger PR gate workflows
        if: github.ref_name == 'main' && steps.cpr.outputs.pull-request-operation != 'none'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          PR_BRANCH: ${{ steps.cpr.outputs.pull-request-branch }}
          BASE_SHA: ${{ github.sha }}
          HEAD_SHA: ${{ steps.cpr.outputs.pull-request-head-sha }}
        run: |
          python3 scripts/dispatch_pr_gate_workflows.py \
            --repo "$REPO" \
            --ref "$PR_BRANCH" \
            --base-sha "$BASE_SHA" \
            --head-sha "$HEAD_SHA"
